# The layer bookkeeping copyinit.m checks is internal to this implementation,
# and Apple does not raise where it does.
# -calculatedAnimationValueAtTime:onLayer: and -applyToLayer: are GNUstep API
# with no counterpart in Apple QuartzCore.
APPLE_SKIP_TESTS="copyinit.m interpolation.m"
